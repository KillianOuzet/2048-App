<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ════════════════════════════════════════════ -->
    <!--         ATTRIBUTS PERSONNALISÉS 2048         -->
    <!-- ════════════════════════════════════════════ -->

    <!-- Arrière-plans -->
    <attr name="bgColor" format="color" />
    <attr name="bgColor2" format="color" />
    <attr name="bgColor3" format="color" />

    <!-- Cartes -->
    <attr name="cardColor" format="color" />
    <attr name="cardColor2" format="color" />
    <attr name="cardColor3" format="color" />

    <!-- Couleurs principales -->
    <attr name="primaryColor" format="color" />
    <attr name="primaryColor2" format="color" />
    <attr name="accentColor" format="color" />
    <attr name="dangerColor" format="color" />

    <!-- Textes -->
    <attr name="textColor" format="color" />
    <attr name="textColor2" format="color" />
    <attr name="textColor3" format="color" />

    <!-- Bordures -->
    <attr name="borderColor" format="color" />
    <attr name="borderColor2" format="color" />

    <!-- Grille -->
    <attr name="tileBgColor" format="color" />

    <!-- Tuiles par valeur -->
    <attr name="tile2Color" format="color" />
    <attr name="tile4Color" format="color" />
    <attr name="tile8Color" format="color" />
    <attr name="tile16Color" format="color" />
    <attr name="tile32Color" format="color" />
    <attr name="tile64Color" format="color" />
    <attr name="tile128Color" format="color" />
    <attr name="tile256Color" format="color" />
    <attr name="tile512Color" format="color" />
    <attr name="tile1024Color" format="color" />
    <attr name="tile2048Color" format="color" />

    <!-- Texte sur les tuiles -->
    <attr name="tileTextDark" format="color" />
    <attr name="tileTextLight" format="color" />

    <!-- Badge -->
    <attr name="badgeBgColor" format="color" />
    <attr name="badgeTextColor" format="color" />

    <!-- Toggle -->
    <attr name="toggleOffColor" format="color" />
    <attr name="toggleOnColor" format="color" />

    <!-- Navigation -->
    <attr name="navColor" format="color" />
    <attr name="navActiveColor" format="color" />

    <!-- Scrollbar -->
    <attr name="scrollbarColor" format="color" />

</resources>
